---
import '../styles/global.css';
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload Successful - Fresh Wax</title>
</head>
<body class="antialiased bg-gray-50">
  <div class="max-w-3xl mx-auto px-6 py-12">
    
    <!-- Header with Logo -->
    <div class="text-center mb-12">
      <div class="mb-8">
        <img 
          src="/logo.webp" 
          alt="FreshWax Logo" 
          class="h-21 mx-auto mb-6"
        />
      </div>
    </div>

    <!-- Success Content -->
    <div class="bg-white border border-gray-200 rounded-lg shadow-sm">
      <div class="p-8 text-center bg-green-50 border-b-2 border-green-200">
        <div class="inline-flex items-center justify-center w-20 h-20 bg-green-100 rounded-full mb-6">
          <svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
          </svg>
        </div>
        <h1 class="text-3xl font-bold text-green-900 mb-3"> Upload Complete! 🎉</h1>
        <p class="text-lg text-green-700">Your release has been uploaded successfully.</p>
      </div>
      
      <div class="p-8">
        <!-- Submission Details -->
        <div class="bg-gray-50 rounded-lg p-6 mb-6 border border-gray-200">
          <div class="text-sm text-gray-600 mb-3 uppercase tracking-wide">Submission Reference</div>
          <div id="refNumber" class="text-3xl font-bold text-gray-900 mb-6 font-mono">Loading...</div>
          <div class="text-sm text-gray-700 space-y-2">
            <div><span class="font-semibold text-gray-900">Artist:</span> <span id="artistName">Loading...</span></div>
            <div><span class="font-semibold text-gray-900">Release:</span> <span id="releaseName">Loading...</span></div>
          </div>
        </div>

        <!-- Next Steps -->
        <div class="bg-blue-50 rounded-lg p-6 mb-6 border border-blue-200">
          <h2 class="font-semibold text-blue-900 mb-4 text-lg">What happens next?</h2>
          <ul class="text-sm text-blue-800 space-y-3">
            <li class="flex items-start">
              <span class="text-xl mr-3">✅</span>
              <span>You'll receive a confirmation email shortly at the address you provided</span>
            </li>
            <li class="flex items-start">
              <span class="text-xl mr-3">🔍</span>
              <span>We'll review your submission and uploaded files</span>
            </li>
            <li class="flex items-start">
              <span class="text-xl mr-3">📧</span>
              <span>We'll contact you if we need any additional information</span>
            </li>
            <li class="flex items-start">
              <span class="text-xl mr-3">🎵</span>
              <span>Once approved, your release will be added to the Fresh Wax store</span>
            </li>
          </ul>
        </div>

        <!-- Important Note -->
        <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
          <p class="text-sm text-yellow-800">
            <span class="font-semibold">📌 Important:</span> Please save your reference number for future reference.
          </p>
        </div>

        <!-- Action Buttons -->
        <div class="flex flex-col sm:flex-row gap-3 justify-center">
          <a
            href="/"
            class="px-8 py-4 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition-colors text-center"
          >
            Submit Another Release
          </a>
          <a
            href="https://freshwax.co.uk"
            class="px-8 py-4 bg-gray-100 text-gray-700 font-semibold rounded-lg hover:bg-gray-200 transition-colors text-center"
          >
            Back to Store
          </a>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <div class="text-center mt-8 text-gray-600 text-sm">
      <p class="mb-2">Questions about your submission?</p>
      <a href="mailto:contact@freshwax.co.uk" class="text-blue-600 hover:text-blue-700 font-medium">
        contact@freshwax.co.uk
      </a>
    </div>
  </div>

  <script>
    // Get URL parameters
    const urlParams = new URLSearchParams(window.location.search);
    const refNumber = urlParams.get('ref');
    const artistName = urlParams.get('artist');
    const releaseName = urlParams.get('release');

    // If no parameters, redirect to home
    if (!refNumber || !artistName || !releaseName) {
      console.warn('Missing URL parameters, redirecting to home');
      window.location.href = '/';
    } else {
      // Populate the page with data
      const refElement = document.getElementById('refNumber');
      const artistElement = document.getElementById('artistName');
      const releaseElement = document.getElementById('releaseName');

      if (refElement) refElement.textContent = decodeURIComponent(refNumber);
      if (artistElement) artistElement.textContent = decodeURIComponent(artistName);
      if (releaseElement) releaseElement.textContent = decodeURIComponent(releaseName);
    }
  </script>
</body>
</html>